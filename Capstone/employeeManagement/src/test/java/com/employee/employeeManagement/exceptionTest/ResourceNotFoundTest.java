package com.employee.employeeManagement.exceptionTest;

import com.employee.employeeManagement.exception.ResourceNotFoundException;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class ResourceNotFoundTest {
//    @Test
//    public void testResourceNotFoundDefaultMessage() {
//        ResourceNotFoundException exception = new ResourceNotFoundException();
//        assertEquals("Not a Valid User", exception.getMessage());
////        assertEquals(HttpStatus.NOT_FOUND, exception.getStatus());
//    }

//    @Test
//    public void testResourceNotFoundCustomMessage() {
//        String messgae = "Not a Valid User";
//        ResourceNotFoundException exception = new ResourceNotFoundException(messgae);
//        assertEquals("Not a Valid User", exception.getMessage());
////        assertEquals(HttpStatus.NOT_FOUND, exception.getStatus());
//    }
}
